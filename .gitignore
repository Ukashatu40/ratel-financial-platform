/node_modules
/generated/prisma
.agents
.claude
.windsurf
.env
docs/architecture.md
example.data.txt
CLAUDE.md

# .gitignore
node_modules/
dist/
coverage/
.env
*.log
.DS_Store
prisma/migrations/dev.db

# ClamAV signature databases, downloaded by the e2e harness's container.
# ~112MB of binary .cvd files that regenerate themselves on demand — must
# never enter git history, where they'd be permanent.
test/.clamav-cache/